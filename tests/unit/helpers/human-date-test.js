import {
  humanDate
} from 'krafty-kai/helpers/human-date';

module('HumanDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = humanDate(42);
  ok(result);
});
