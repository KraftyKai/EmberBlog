import {
  markupText
} from 'krafty-kai/helpers/markup-text';

module('MarkupTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = markupText(42);
  ok(result);
});
